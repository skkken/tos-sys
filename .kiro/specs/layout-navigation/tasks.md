# Implementation Plan

- [x] 1. shadcn/ui Sidebarコンポーネントのインストールと既存ルート構成の再編
- [x] 1.1 shadcn/ui Sidebarコンポーネントを追加し、依存コンポーネント（Sheet, Collapsible等）が正しくインストールされることを確認する
  - `pnpm dlx shadcn@latest add sidebar` を実行
  - `components/ui/sidebar.tsx` が生成されることを確認
  - 依存コンポーネントが自動追加されることを確認
  - _Requirements: 5.1_

- [x] 1.2 既存の `app/receipts/` ディレクトリを `app/(app)/receipts/` に移動し、`(app)` / `(auth)` ルートグループ構造を作成する
  - `app/(app)/` と `app/(auth)/` ルートグループディレクトリを作成
  - 既存の `app/receipts/` を `app/(app)/receipts/` に移動
  - APIルート（`app/api/`）は移動不要であることを確認
  - 移動後にビルドが通ることを確認
  - _Requirements: 5.2, 6.1_

- [x] 2. 認証付きサイドバーレイアウトの構築
- [x] 2.1 `(app)` ルートグループにSidebarProvider付きの共通レイアウトを作成し、サイドバーとメインコンテンツ領域を構成する
  - SidebarProvider, Sidebar, SidebarTriggerを使用してレイアウトを構成
  - メインコンテンツ領域に子ページをレンダリング
  - デスクトップではサイドバー常時表示、モバイルではハンバーガーメニューに切り替わることを確認
  - 既存のmiddleware.tsによる認証ガードが `(app)` 配下のページに正しく適用されることを確認（middleware自体は実装済み）
  - _Requirements: 2.1, 2.2, 4.1, 4.2, 4.3, 5.1, 5.2_

- [x] 2.2 9項目のナビゲーションを持つサイドバーコンポーネントを作成し、現在のページに対応する項目をアクティブハイライトする
  - ダッシュボード、タスク、請求書、見積もり、収支、経費、キャッシュフロー、顧客、契約の9項目をナビゲーションに定義
  - 各項目にアイコンとリンクを設定
  - 現在のパスに基づいてアクティブ項目をハイライト表示
  - SidebarMenu, SidebarMenuItem, SidebarMenuButtonを使用
  - _Requirements: 3.1, 3.2, 3.3_

- [x] 3. 各機能ページのプレースホルダー作成とルートリダイレクト
- [x] 3.1 (P) 9つの機能ページ（ダッシュボード、タスク、請求書、見積もり、収支、経費、キャッシュフロー、顧客、契約）のプレースホルダーを作成する
  - 各ページはサーバーコンポーネントとしてページタイトルを表示する最小限の構成
  - `(app)` ルートグループ配下に配置
  - _Requirements: 6.1, 6.3_

- [x] 3.2 (P) ルートパス `/` にアクセスした際に `/dashboard` へリダイレクトする処理を実装する
  - `(app)` 配下のルートページで `redirect('/dashboard')` を使用
  - _Requirements: 6.2_

- [x] 4. ログインページの構築
- [x] 4.1 ログインフォームコンポーネントを作成し、メールアドレス・パスワード入力とSupabase認証を実装する
  - メールアドレス入力フィールド、パスワード入力フィールド、ログインボタンを表示
  - Supabase Authの `signInWithPassword` で認証処理を実行
  - 認証成功時にダッシュボードへリダイレクト
  - 認証失敗時にエラーメッセージを表示
  - ローディング中はボタンを無効化しローディング表示、二重押下を防止
  - _Requirements: 1.1, 1.2, 7.1, 7.3_

- [x] 4.2 `(auth)` ルートグループにログインページを配置し、認証済みユーザーのリダイレクト処理を実装する
  - `(auth)/login/` にページを配置（サイドバーレイアウトを適用しない）
  - サーバーサイドで認証状態をチェックし、認証済みの場合は `/dashboard` へリダイレクト
  - 未認証の場合はログインフォームを表示
  - _Requirements: 1.3, 7.2_

- [x] 5. 統合確認とビルド検証
- [x] 5.1 全ページの認証ガード、サイドバーナビゲーション、レスポンシブ動作が正しく機能することを確認し、`pnpm build` が成功することを検証する
  - 未認証アクセスが `/login` にリダイレクトされることを確認
  - ログイン後にダッシュボードが表示されることを確認
  - サイドバーの9項目からの遷移とアクティブハイライトを確認
  - モバイル幅でのハンバーガーメニュー動作を確認
  - `pnpm build` が成功することを確認
  - _Requirements: 2.1, 2.2, 3.1, 3.2, 3.3, 4.1, 4.2, 4.3_
